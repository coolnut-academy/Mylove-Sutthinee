# ผลตรวจโปรเจกต์ 12 กันยายน 2569

## ขอบเขตและผลตรวจ

ตรวจ syntax ของ JavaScript 44 ไฟล์และ Apps Script ทุกไฟล์ ตรวจการเรียก API จากทุกโมดูลหน้าเว็บ และตรวจเส้นทางไฟล์ใน HTML 173 จุด ไม่พบไฟล์ปลายทางหาย

## ข้อผิดพลาดที่แก้

- ปุ่มแก้ไขหัวข้อ ว.PA เรียก `updateSection` ที่ไม่มี implementation: เพิ่มครบ repository, live/mock provider และ backend พร้อมอ่านชีตกลับมายืนยันก่อนแจ้งสำเร็จ
- ผูกการแก้หัวข้อกับปีตอนเปิดแบบฟอร์ม รักษาคำอธิบายเดิมและคอลัมน์อื่น รองรับการสร้างแถวของหน้าประกอบ เช่น profile ด้วย ID คงที่เพื่อไม่สร้างซ้ำ
- แก้ข้อความที่มีเครื่องหมายคำพูดให้แสดงในแบบฟอร์มได้ถูกต้อง
- หน้าจัดการโหลดค่าตั้งค่าและปีซ้ำหลายรอบ: รวมงานอิสระให้โหลดพร้อมกัน ทำให้ provider รวมคำขอซ้ำได้ และอ่านเฉพาะหมวด PA ที่หน้านี้ใช้
- ป้องกันผลโหลดหมวดปีเก่าทับปีใหม่ และรักษาหมวดที่เลือกจากปุ่มเพิ่มหลักฐานไว้จนโหลดตัวเลือกเสร็จ เปลี่ยนปีในกล่องอัปโหลดแล้วโหลดหมวดของปีนั้น
- ไม่ให้เริ่มอัปโหลดเมื่อยังโหลดตัวเลือกอยู่ โหลดผิดพลาด หรือยังไม่ได้เลือกหมวด
- ป้องกันคำขอเก่าที่จบหลังบันทึกนำแคชที่เพิ่งล้างกลับมาอีก
- แก้ legacy inline editor ของเกม หนังสือ และประเด็นท้าทายที่กลืนข้อผิดพลาดการบันทึก ให้คงแบบฟอร์มและไม่แจ้งสำเร็จเมื่อบันทึกล้มเหลว
- ป้องกันการผูกปุ่มเปิดหน้าห้องเรียนซ้ำเมื่อเข้าใช้งานหน้าจัดการหลายรอบ
- ยกเลิกรหัสผ่านสำรองและการนำค่า hash มาใช้แทนรหัสผ่านเพื่อข้ามรหัสที่ตั้งไว้ และสร้างกุญแจ session แบบสุ่มเก็บใน Script Properties เมื่อยังไม่มี โดยรักษากุญแจที่ตั้งไว้แล้ว

## หลักฐานด้านความเร็ว

ใช้ Chrome และข้อมูล API จำลองชุดเดียวกันเปรียบเทียบหน้าเว็บก่อนแก้กับหลังแก้ โดยไม่ใช้แคชเดิม:

| การเปิดหน้าจัดการ | ก่อน | หลัง |
| --- | --- | --- |
| จำนวนคำขอ Google | 5 | 2 |
| คำขอ | bootstrap 3, sections 1, PA data 1 | bootstrap 1, sections 1 |

ลดจำนวนคำขอ 60% ไม่ได้หมายความว่าเวลาโหลดจะลดลง 60% ทุกครั้ง เวลาเครือข่ายและการเริ่มทำงานของ Apps Script ยังแปรผันได้ ตรวจ bootstrap จริงขนาดประมาณ 2 KB และไม่พบภาพ base64 ในข้อมูลตั้งค่าปัจจุบัน

## การทดสอบ

```powershell
node tests/project-audit.mjs
node tests/storage-pipeline.mjs
node tests/read-performance.mjs
node tests/save-completion.mjs
node tests/logout.mjs
node tests/loading-progress.mjs
node tests/verify-all.js
```

ทุกชุดผ่าน รวมชุดตรวจเดิม 100 ข้อ ทดสอบเบราว์เซอร์เพิ่มเติม: เปิดแบบฟอร์มพร้อมคำอธิบายเดิมและข้อความที่มีเครื่องหมายคำพูด บันทึกล้มเหลวแล้วลองใหม่ เลือกหมวดจากปุ่มเพิ่มหลักฐาน และเปลี่ยนปีอัปโหลด

การทดสอบบันทึกหัวข้อใช้บริการ Sheets จำลองกับซอร์ส backend จริง และการทดสอบ UI ใช้ API จำลอง ไม่แก้หัวข้อจริงของผู้ใช้เพื่อทดสอบ

## ข้อจำกัดที่ยังต้องติดตาม

- หากยังไม่ได้ตั้ง `ADMIN_PASSWORD_HASH` ระบบยังใช้รหัสเริ่มต้นของโครงการ ควรตั้งรหัสส่วนตัวใน Script Properties การแก้ครั้งนี้รักษาค่าที่ตั้งไว้แล้ว
- การเปลี่ยนภาพในหน้าตั้งค่าบางเส้นทางยังส่ง base64 ลง SETTINGS; ภาพใหญ่มีโอกาสเกินขีดจำกัดเซลล์ ปัจจุบันฐานข้อมูลใช้ URL รูปภาพ จึงไม่ใช่คอขวดที่วัดพบรอบนี้
- งานอัปโหลดที่ Drive สำเร็จแต่ขั้นบันทึกรายการล้มเหลวบางเส้นทางอาจเหลือไฟล์ใน Drive
- ผลตรวจนี้ไม่ใช่การรับประกันว่าปราศจากข้อผิดพลาดทุกกรณี หรือการทดสอบโหลดพร้อมกันจำนวนมาก
